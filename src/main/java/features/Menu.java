 package features;

//import features.BusinessFeature;
import java.util.Scanner;
public class Menu {

 /*	public static void main(String[] args) {
		// TODO Auto-generated method stub
		Scanner scanner = new Scanner(System.in);
		int employeeId, supervisorId;
		String name, localauth;
		System.out.println("Select A - List all distinct (unique) Local Authorities for which you have data.");
		localauth = scanner.nextLine();
		scanner.nextLine(); //This is needed to pick up the new line
		System.out.println("Enter employee name:");
		name = scanner.nextLine();
		System.out.println("Enter supervisor ID:");
		supervisorId = scanner.nextInt();
		//	ask();
//	}

		public  static void ask() {

			Scanner sc = new Scanner(System.in);
			System.out.println("Welcome to Console App. You can test the features." +
					" \n What do you want to do? \n" +
					"Enter a number to test the features \n" +
					"1. List all distinct (unique) Local Authorities for which you have data " +
					"2. List all distinct (unique) businesses (BusinessName) for which you have data" +
					"3. List all premises ratings (Ratings and Scores), for a user-specified business, in chronological order of\n" +
					"rating date (most recent first)\n" +
					"4. List output details, where a rating is:\n" +
					"i. above a specified value\n" +
					"ii. below a specified value\n" +
					"iii. within a specified range (e.g. \"min 3, max 5\")\n" +
					"iv. a special value (e.g. exempt or pending)\n" +
					"");
			while (sc.hasNext()) {
				int select = 0;
				if (sc.hasNextInt()) {
					select = sc.nextInt();
				}
				switch (select) {

					case 1:
						//CsvReader.main();
						BusinessFeature.getBusinessNamesList();
						break;
					case 2:
					//	CsvReader.busNameSet();
						break;
					case 3:

						break;

					default:
						System.out.println("choose from 1 to 3");
						ask();

				}
			}

	}
*/
}

